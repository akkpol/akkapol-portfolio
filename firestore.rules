rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Profiles collection - allow read for everyone, write for authenticated users
    match /profiles/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

